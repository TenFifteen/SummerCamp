/**
 * Problem: a + b
 * Solve: when a and b are too large.
 * Tips: no
 */
#include <iostream>

using namespace std;

int main()
{
    int a, b;
    while (cin >> a >> b) {
        cout << a + b << endl;
    }

    return 0;
}
